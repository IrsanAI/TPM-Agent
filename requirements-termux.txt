# Termux-safe dependency profile
# Heavy scientific packages must come from Termux pkg to avoid slow/fragile source builds.
# Install first: pkg install -y python-numpy
fastapi>=0.95,<0.100
pydantic>=1.10,<2
uvicorn>=0.23,<0.31
PyYAML>=6.0.1
